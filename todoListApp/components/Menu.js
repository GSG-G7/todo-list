import React from 'react';

import {Text, View} from 'react-native';
import Icon from 'react-native-vector-icons/FontAwesome';

const Menu = () => {
  return <Icon name="bars" size={30} color="red" />;
};

export default Menu;
